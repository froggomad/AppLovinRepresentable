import XCTest
@testable import AppLovinRepresentable

final class AppLovinRepresentableTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(AppLovinRepresentable().text, "Hello, World!")
    }
}
